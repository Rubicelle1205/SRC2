@model WebPccuClub.Models.BorrowMailTemplateMangViewModel

@{
    Layout = "_BackendLayout";
}

<head>
    <script src="~/js/backend_case.js"></script>
</head>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <!-- /.row -->
        <div class="row">
            <div class="col-12">
                <form method="post" id="form" enctype="multipart/form-data" action="">

                    <div class="card">
                        <div class="card-body table-responsive-sm">
                            <h6 class="card-subtitle mb-2 text-muted">
                                <a id="Back" href="@Url.Action("Index")" class="btn btn-outline-primary">返回</a>
                                <span class="required font-weight-bold">*</span> <small>為必填欄位</small>
                            </h6>
                            <table class="table table-bordered">
                                <tbody>
                                    <tr>
                                        <th width="15%">@Html.LabelFor(m => m.EditModel.Created)</th>
                                        <td>@Html.DisplayFor(m => m.EditModel.Created)</td>
                                    </tr>
                                    <tr>
                                        <th width="15%">@Html.LabelFor(m => m.EditModel.LastModified)</th>
                                        <td>@Html.DisplayFor(m => m.EditModel.LastModified)</td>
                                    </tr>
                                    <tr>
                                        <th width="10%">範本名稱<span class="required">*</span></th>
                                        <td>@Html.TextBoxFor(m => m.EditModel.TemplateName, new { @class = "form-control", placeholder = "請輸入範本名稱" })</td>
                                    </tr>
                                    <tr>
                                        <th width="10%">借用類別<span class="required">*</span></th>
                                        <td>
                                            @Html.DropDownListFor(m => m.EditModel.BorrowMainClassID, (List<SelectListItem>)ViewBag.ddlMainClass, "請選擇借用類別", new { @class = "form-control" })
                                        </td>
                                    </tr>
                                    <tr>
                                        <th width="10%">是否啟用<span class="required">*</span></th>
                                        <td>
                                            @Html.DropDownListFor(m => m.EditModel.IsEnable, (List<SelectListItem>)ViewBag.ddlEnable, "是否啟用", new { @class = "form-control" })
                                        </td>
                                    </tr>
                                    <tr>
                                        <th width="10%">信件主旨<span class="required">*</span></th>
                                        <td>@Html.TextBoxFor(m => m.EditModel.SubjectTemplate, new { @class = "form-control", placeholder = "請輸入信件主旨" })</td>
                                    </tr>
                                    <tr>
                                        <th width="10%">信件內容<span class="required"></span></th>
                                        <td>@Html.TextAreaFor(m => m.EditModel.BodyTemplate, new { @class = "form-control", placeholder = "請輸入信件內容", rows = "5" })</td>
                                    </tr>
                                </tbody>
                            </table>

                        </div>

                        <div class="card-footer d-flex justify-content-end">
                            <button type="button" class="btn btn-success" onclick="SaveData()">儲存</button>
                        </div>
                    </div>
                    <!-- /.card -->
                    @Html.HiddenFor(m => m.EditModel.TemplateId)
                </form>
                <!-- /.form -->
            </div>
        </div>
        <!-- /.row -->


    </div><!-- /.container-fluid -->
</section>
<!-- /.content -->

<script>
    function SaveData() {

        var data = new FormData()

        var TemplateId = $('#@Html.IdFor(m => m.EditModel.TemplateId)').val();
        var TemplateName = $('#@Html.IdFor(m => m.EditModel.TemplateName)').val();
        var BorrowMainClassID = $('#@Html.IdFor(m => m.EditModel.BorrowMainClassID)').val();
        var IsEnable = $('#@Html.IdFor(m => m.EditModel.IsEnable)').val();
        var SubjectTemplate = $('#@Html.IdFor(m => m.EditModel.SubjectTemplate)').val();
        var BodyTemplate = $('#@Html.IdFor(m => m.EditModel.BodyTemplate)').val();

        if (TemplateName == "") { $.alert("範本名稱不可為空白"); return; }
        if (BorrowMainClassID == "") { $.alert("借用類別需選擇"); return; }
        if (IsEnable == "") { $.alert("是否啟用需選擇"); return; }
        if (SubjectTemplate == "") { $.alert("信件主旨不可為空白"); return; }
        if (BodyTemplate == "") { $.alert("信件內容不可為空白"); return; }

        data.append('@Html.NameFor(m => m.EditModel.TemplateId)', TemplateId);
        data.append('@Html.NameFor(m => m.EditModel.TemplateId)', TemplateId);
        data.append('@Html.NameFor(m => m.EditModel.TemplateName)', TemplateName);
        data.append('@Html.NameFor(m => m.EditModel.BorrowMainClassID)', BorrowMainClassID);
        data.append('@Html.NameFor(m => m.EditModel.IsEnable)', IsEnable);
        data.append('@Html.NameFor(m => m.EditModel.SubjectTemplate)', SubjectTemplate);
        data.append('@Html.NameFor(m => m.EditModel.BodyTemplate)', BodyTemplate);

        SendAjax({
            url: '@Url.Action("EditOldData")',
            data: data,
            processData: false,
            contentType: false,
            success: function (result) {
                if (result.errorCode == 0) {
                    $.confirm({
                        title: "",
                        content: "修改成功",
                        buttons: {
                            確定: function () {
                                window.location = $("#Back").attr("href");
                            },
                        }
                    });
                }
                else { $.alert(result.errorMsg); }
            },
        });
    }

    function Reset() {
        $('#@Html.IdFor(m => m.EditModel.TemplateName)').val("");
        $('#@Html.IdFor(m => m.EditModel.BorrowMainClassID)').val("");
        $('#@Html.IdFor(m => m.EditModel.IsEnable)').val("");
        $('#@Html.IdFor(m => m.EditModel.SubjectTemplate)').val("");
        $('#@Html.IdFor(m => m.EditModel.BodyTemplate)').val("");
    }
</script>